apiVersion: v1
kind: Service
metadata:
  name: kube-bar-svc
  namespace: default
spec:
  type: NodePort
  selector:
    app: kube-bar-app
  ports:
    - port: 3002
      targetPort: 8001
      nodePort: 32002
      protocol: TCP

