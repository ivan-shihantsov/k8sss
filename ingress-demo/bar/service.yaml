apiVersion: v1
kind: Service
metadata:
  name: kube-bar-svc
  namespace: default
spec:
  type: ClusterIP
  selector:
    app: kube-bar-app
  ports:
    - port: 3002
      targetPort: 8001
      protocol: TCP

